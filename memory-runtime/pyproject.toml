[project]
name = "codex-memory-runtime"
version = "0.1.0"
description = "Per-project long-term memory runtime for Codex"
requires-python = ">=3.12"
dependencies = [
  "protobuf>=5.28,<6.0",
  "sentencepiece>=0.2,<0.3",
  "sentence-transformers>=3.0,<4.0",
  "sqlite-vec==0.1.7",
]
